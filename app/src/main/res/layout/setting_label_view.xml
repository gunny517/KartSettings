<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="label"
            type="String" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="@dimen/setting_item_view_width"
        android:layout_height="@dimen/setting_item_view_height"
        android:background="@drawable/bg_setting_item">

        <TextView
            android:layout_width="@dimen/setting_item_text_view_width"
            android:layout_height="@dimen/setting_item_text_view_height"
            android:gravity="center_vertical"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:padding="@dimen/padding_s"
            android:textSize="@dimen/text_size_s"
            android:text="@{label}"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>