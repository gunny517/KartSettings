package jp.ceed.kart.settings.ui.navigation

/**
 * 画面遷移パラメータ名定義
 * mobile_navigation.xml の argument 要素の name 属性値に合わせて定義しておく
 * （各所でハードコーディングしたくないので定義しておく）
 */
enum class AppNavArgs {
    PRACTICE_ID,
    TITLE_LABEL;
}